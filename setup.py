from setuptools import setup, find_packages

setup(
    name='mrcnn',  # Replace with your desired package name
    version='0.1.0',  # Replace with your version
    packages=find_packages(),
    install_requires=[  # List any external dependencies here
        'tensorflow',
        # ... other dependencies
    ],
)
